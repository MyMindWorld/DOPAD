import random
import math
import time


def is_debugger_present():
    return round(random.random())


def is_loaded_process(Name):
    return round(random.random())


x = input('X')
c = 6
f = 23
j = c * f
y = input('Y')
p = j / f * int(x)
if round(random.random()) == 1:
    print(math.sin(int(x))+math.sin(int(y)))
    test = 1
if round(random.random()) == 0:
    print(math.sin(int(x))+math.sin(int(y)))
    test = 0
z = 6 * f - c
if is_debugger_present() is True:
    '''plant.bomb
        kill all humans
            explode the bomb
                create the great society without meat
                    stop editing PEP8
    '''
    exit(666)
start_time = time.time()
p = j / f * int(x)
math.sin(int(x))+math.sin(int(y))
time.sleep(random.random())
end_time = time.time()
if end_time - start_time > 1:
    '''plant.bomb
            kill all humans
                explode the bomb
                    create the great society without meat
                        stop editing PEP8
        '''
    exit(666)
else:
    print("Never know what %s seconds can change" % (1 - (end_time - start_time)))

if is_loaded_process('pdb') is True:
    '''plant.bomb
                kill all humans
                    explode the bomb
                        create the great society without meat
                            stop editing PEP8
            '''
    exit(666)
